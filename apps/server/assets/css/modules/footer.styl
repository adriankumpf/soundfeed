@import '../variables'

.footer {
  display: flex;
  justify-content: space-between;
  width: $width;
  max-width: $maxWidth;
  font-size: 12px;
  letter-spacing: 5px;

  &-item {
    color: $main;
    text-decoration: none;
    font-weight: 400;
    text-transform: uppercase;

    &:hover {
      color: darken($main, 15%)
    }
  }
}
