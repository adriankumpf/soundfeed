@import '../variables'

.footer {
  display: flex;
  width: 100%
  justify-content: space-between;
  width: $maxWidth;

  &-item {
    color: $orange;
    font-weight: 400;
    font-size: 12px;
    text-decoration: none;
    text-transform: uppercase;
  }
}
